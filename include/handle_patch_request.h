#ifndef HANDLE_PATCH_REQUEST_H
#define HANDLE_PATCH_REQUEST_H

#include "common.h"

char* handle_patch_request(const char *url, const char *upload_data, size_t *upload_data_size);

#endif // HANDLE_PATCH_REQUEST_H
